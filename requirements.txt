numpy>=1.24.0
scipy>=1.10.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
websockets>=11.0
opencv-python-headless>=4.8.0
sounddevice>=0.4.6
Pillow>=10.0.0

# Screen watching (optional but strongly recommended)
# - mss: 10x faster screen capture
# - pytesseract: OCR text reading (ALSO requires Tesseract binary - see install notes)
# - pygetwindow: active window detection
mss>=9.0.0
pytesseract>=0.3.10
pygetwindow>=0.0.9

# ------------------------------------------------------------------
# V1.3 additions
# ------------------------------------------------------------------

# Security — AES-256-GCM encryption for brain_state
# (If missing, security falls back to a stdlib HMAC-stream cipher.)
cryptography>=41.0.0

# Agent tools — Excel (.xlsx) support. CSV works without this.
openpyxl>=3.1.0

# Desktop tray launcher (optional — falls back to console mode)
pystray>=0.19.0

# Optional: richer benchmark hardware reporting
psutil>=5.9.0

# Voice pipeline premium backends — all OPTIONAL (mock fallbacks built in).
# Uncomment to enable:
# silero-vad>=5.0
# faster-whisper>=0.10
# piper-tts>=1.2
